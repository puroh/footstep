services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: footstep
      POSTGRES_USER: footstep
      POSTGRES_PASSWORD: footstep
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U footstep"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: ./footstep_backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.development
      POSTGRES_DB: footstep
      POSTGRES_USER: footstep
      POSTGRES_PASSWORD: footstep
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      DJANGO_SECRET_KEY: dev-secret-key-not-for-production
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./footstep_backend:/app
      - /app/.venv

  frontend:
    build:
      context: ./footstep_frontend
      dockerfile: Dockerfile
    ports:
      - "4321:4321"
    environment:
      PUBLIC_API_BASE: http://localhost:8000/api/v1
      API_BASE_INTERNAL: http://backend:8000/api/v1
    depends_on:
      - backend

volumes:
  postgres_data:
